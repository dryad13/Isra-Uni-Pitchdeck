# OMR Engine (vendored)

Vendored copy of [udayraj123/OMRChecker](https://github.com/udayraj123/OMRChecker) **v1.1.0** (MIT).

Source lives in `src/`. Install runtime extras before running the POC:

```bash
pip install -r backend/omr_engine/requirements-vendor.txt
```

Used by the OMR read pipeline (M09) and Part 0 POC (`backend/scripts/run_poc.py`).

License: see `LICENSE`.
